<div class="prompt-button">
    <button data-id="{{ field.id }}" data-field="{{ widget.name }}" data-object-id="{{ objectId }}" data-prefix="{{ path('contao_backend') }}" type="button">{{ field.parent.title }}</button>
</div>